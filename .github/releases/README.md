# Per-tag release notes (optional)

If a file named after the git tag exists here, the release workflow uses it
instead of the default template in `../release-notes.md`.

Example: `v0.3.7.md` for tag `v0.3.7`.

Write normal GitHub-flavored Markdown. Do not escape newlines as `\n`.
